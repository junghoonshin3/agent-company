{
  "name": "agent-company",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A local Codex plugin that runs a CEO-led product team with Codex sub-agents and a project-local discussion server.",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test server/test/*.test.ts",
    "validate": "node scripts/validate-plugin.mjs",
    "check": "npm run validate && npm test"
  }
}
